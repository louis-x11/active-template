import {request2Get, request2Post} from "@/utils/request"

export function toGetData() {
  // return request2Get({
  //    url: '',
  //    params: {}
  // })
  return Promise.reject('测试request模块')
}
